import styled from '@emotion/styled'

export const SDiv = styled.div`
  display: flex;
  padding: 2rem;
`;

export const SImg = styled.img`
  height: 4rem;
  width: 4rem;
`;

export const SButton = styled.button`
  border: none;
  outline: none;
  background: none;
  padding: 0 0.2rem;
`;

export const SFlyingEmoji = styled.div`
  width: 4rem;
  height: 4rem;
  position: absolute;
  bottom: 0;
`;